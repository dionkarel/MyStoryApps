<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintTop_toTopOf="parent"
    app:cardCornerRadius="15sp"
    android:layout_margin="8dp"
    android:layout_marginVertical="15dp"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/iv_getStory"
            android:layout_width="0dp"
            android:layout_height="200dp"
            android:layout_margin="28dp"
            android:scaleType="centerCrop"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:src="@tools:sample/avatars"
            tools:ignore="ContentDescription" />

        <TextView
            android:id="@+id/tv_getUsername"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="15dp"
            tools:text="Username"
            android:textStyle="bold"
            app:layout_constraintStart_toStartOf="@+id/iv_getStory"
            app:layout_constraintTop_toBottomOf="@+id/iv_getStory" />

        <TextView
            android:id="@+id/tv_getDesc"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="12dp"
            tools:text="Detail Story"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="@+id/tv_getUsername"
            app:layout_constraintTop_toBottomOf="@+id/tv_getUsername" />

        <TextView
            android:id="@+id/tv_getDatePost"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="Datepost"
            app:layout_constraintBottom_toBottomOf="@+id/tv_getUsername"
            app:layout_constraintEnd_toEndOf="@+id/iv_getStory"
            app:layout_constraintTop_toTopOf="@+id/tv_getUsername" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>